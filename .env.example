# ===============================
# Banco de Dados PostgreSQL
# ===============================

# URL completa para conexão com o banco de dados (usuário:senha@host:porta/dbname)
# Em produção, substitua 'localhost' pelo hostname ou IP do serviço de DB real
DATABASE_URL=postgresql://trackchat:trackchat@localhost:5432/trackchat

# Usuário do PostgreSQL (usado pelo docker-compose ou serviço DB)
POSTGRES_USER=trackchat

# Senha do PostgreSQL
POSTGRES_PASSWORD=trackchat

# Nome do banco de dados
POSTGRES_DB=trackchat


# ===============================
# Configuração do ambiente
# ===============================

# Defina o ambiente da aplicação
# Apenas valores aceitos: development, production, test
NODE_ENV=development

# Ambiente interno da aplicação, útil para diferenciar local, staging, prod
# (opcional, se você quiser diferenciar sem alterar NODE_ENV)
APP_ENV=local


# ===============================
# Frontend (Next.js / Socket.IO client)
# ===============================

# URL do servidor de WebSocket que o frontend vai conectar
# Em produção, usar wss:// ou domínio real
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000


# ===============================
# Backend / Webhook
# ===============================

# URL que o backend usa para enviar eventos para o Socket Server
SOCKET_SERVER_URL=http://localhost:4000

# URL interna usada entre containers no docker-compose
# Em produção, normalmente não é usada, a menos que tudo esteja na mesma rede Docker
SOCKET_SERVER_INTERNAL_URL=http://socket:4000
